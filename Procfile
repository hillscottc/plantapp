client: cd client && npm start

## Use the datahub api server.
## Don't know if will work on Heroku yet.
server: cd ../datahub && npm start

## Use the local api server code
## The local api server code will be redundant
## if I can make datahub server work on Heroku deploy.

#server: babel-node server


